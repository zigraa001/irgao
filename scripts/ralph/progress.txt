# Ralph Progress Log
Started: Sun Jun 28 00:00:00 UTC 2026
---

## Codebase Patterns
- Backend is plain Express 5 + raw MySQL via `mysql2` (src/db.js exports `query`, `queryOne`). No ORM.
- Route modules live in src/*-routes.js, each `express.Router()`, mounted in src/api.js under /api/<area>. `express.json()` is applied in server.js.
- Auth utilities in src/auth.js: `hashPassword`/`verifyPassword` (bcrypt, 10 rounds), `signToken`/`verifyToken` (HMAC token), middleware `requireAuth` (sets req.user={id,name,role}) and `requireRole(role)` factory. Compose as `requireAuth, requireRole('admin')`.
- Convention: every route that returns a user shapes it with a local `publicUser(u)` => {id,name,email,role} — NEVER return passwordHash. Emails are lowercased before insert/lookup. EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/, password min length 6.
- Insert pattern: `INSERT ...` returns `{ insertId }`, then re-read with `SELECT * FROM users WHERE id = ?` so the response reflects DB defaults/timestamps.
- Tests use node's built-in runner (`npm test` = `node --test`), no supertest. They are DB-FREE. To test a route without a real MySQL, inject an in-memory fake into `require.cache[require.resolve('../src/db')]` BEFORE requiring the route module (route modules destructure `{query,queryOne}` at load time), mount on a throwaway express app, and drive with real `fetch`. Auth is real: set process.env.AUTH_SECRET then use signToken to mint tokens. See test/admin-routes.test.js.
- Typecheck = `npm run typecheck` (tsc --noEmit, allowJs, checkJs:false, strict:false). Keep it green.
- Frontend is a single self-contained `app.html` (~3000 lines): one inline `<style>`, top-level `.view` divs (login/booking/operator/admin), shown one-at-a-time via `showView(viewId)` (toggles `.active`). After auth, `routeForRole(user)` switches on `user.role` to pick the view, set the avatar initial + welcome line, and kick off that view's data load. Reuse existing `op-*` CSS classes (op-body, op-welcome, op-section-title, op-empty) for new dashboard sections. Shared authed fetch = `apiFetch(path, opts)` (adds Bearer header, auto-logout on 401). Auth-form helpers: `showAuthError/hideAuthError(id)`, `setBusy(btnId,busy,busyLabel,idleLabel)`, `escapeHtml(s)`. Typecheck does NOT cover app.html (it's HTML, not in tsconfig) — verify JS there by reading/running.

## Sun Jun 28 - US-001 Admin-only create-user API
- Added src/admin-routes.js: `POST /api/admin/users` guarded by requireAuth + requireRole('admin'). Validates required name/email/password, EMAIL_RE, password>=6, and role in {operator,admin} (rejects 'customer'/unknown with 400). 409 on existing lowercased email. Stores bcrypt hash via hashPassword; returns 201 { user: {id,name,email,role} } (no passwordHash).
- Mounted the router at /api/admin in src/api.js.
- Added test/admin-routes.test.js (12 tests: 401/403 guards, all validation branches, happy path for operator+admin, passwordHash never leaked, 409 duplicate). Full suite: 29 pass. Typecheck clean.
- **Learnings for future iterations:**
  - The require.cache db-fake trick (see Codebase Patterns) is the way to integration-test admin/list/booking routes without MySQL. The fake matches on whitespace-collapsed SQL prefixes; keep route SQL predictable.
  - requireRole only checks `req.user.role !== role` for a SINGLE role string. There is no built-in "any of these roles" helper yet — add one if a route needs to allow multiple roles.
  - Pre-existing unrelated working-tree changes (scripts/seed.js, scripts/ralph/archive/, tasks/*.md) are harness/env artifacts — keep feature commits focused on src/ + test/ + PRD/progress.
---

## Sun Jun 28 - US-003 Backend signup hardening (defense in depth)
- No production change needed: src/auth-routes.js signup already destructures only `{ name, email, password }` and hard-codes `role = "customer"` in the INSERT, so any client-supplied `role` is ignored by construction.
- Added test/auth-routes.test.js (3 tests) using the require.cache db-fake pattern (taught it the signup `SELECT id FROM users WHERE email`, `INSERT`, and `SELECT * FROM users WHERE id` queries): customer happy path, `{role:'admin'}` still yields a customer (asserts BOTH response and stored row), and `{role:'operator'}` likewise. Existing test/auth.test.js untouched (utility-only, still passes). Full suite: 37 pass. Typecheck clean.
- **Learnings for future iterations:**
  - The signup route is the authoritative role gate: elevated roles come only from src/admin-routes.js. Keep signup's destructure narrow — never spread `req.body` into the INSERT.
  - The db-fake needs `FROM users WHERE email` to return the FULL row for login (`SELECT *`), separate from the `SELECT id FROM users WHERE email` existence check — order the branches id-first so the existence check matches before the full-row branch.
---

## Sun Jun 28 - US-002 Admin-only list-users API
- Added `GET /api/admin/users` to src/admin-routes.js (requireAuth + requireRole('admin')). Returns `{ users: [{id,name,email,role,createdAt}] }` ordered by `createdAt DESC, id DESC`. Optional `?role=` filter validated against new FILTERABLE_ROLES = [operator,admin,customer] (customer IS allowed for listing, unlike CREATABLE_ROLES for create); invalid/absent role → all users.
- Selected explicit columns (no `SELECT *`) so passwordHash can never leak. Added `listUser()` shaper alongside `publicUser()`.
- Extended test/admin-routes.test.js: taught the in-memory db fake to handle the list SELECT (with optional WHERE role + newest-first sort) and added 5 tests. Full suite: 34 pass. Typecheck clean.
- **Learnings for future iterations:**
  - The db-fake matches by SQL prefix; list query starts `SELECT id, name, email, role, createdAt FROM users` — distinct enough not to collide with the existing `WHERE email` / `WHERE id` branches. When adding new SQL, keep the leading shape unambiguous.
  - Filterable vs creatable roles differ on purpose: customers can be LISTED but never CREATED via /api/admin/users.
---


## Sun Jun 28 - US-004 Admin Dashboard view shell + role routing
- Added `<div id="admin-view" class="view">` in app.html mirroring operator-view: booking-nav with logo, new purple `.admin-role-badge` reading "Admin Console", and `#admin-avatar` (with `.admin-avatar` gradient) wired to `logout()`. Body reuses op-body/op-welcome/op-section-title; `#admin-users` is the container the user list (US-005) renders into.
- Extended `routeForRole()` with an `admin` case: sets the avatar initial + `#admin-welcome` line, `showView('admin-view')`, and calls `loadAdminUsers()`. Admins no longer fall through to the booking view. Customer/operator branches untouched.
- Added a minimal placeholder `loadAdminUsers()` (shows "Loading users…"); US-005 replaces its body with the real GET /api/admin/users fetch + render.
- Added CSS: `#admin-view` (column flex, gray-50 bg), `.admin-role-badge` (purple), `.admin-avatar` (purple→blue gradient) to visually distinguish admin from operator.
- Typecheck clean; full suite 37 pass (no backend change, so test count unchanged).
- **Learnings for future iterations:**
  - app.html views are pure show/hide; adding a role view = (1) a `.view` div, (2) a `routeForRole` case, (3) a loader fn. No router lib.
  - Browser verification could NOT be done here (no dev-browser/MCP browser tool configured). US-004/005/006/007 acceptance all ask for it — a human must manually confirm admin@irago.com / iragoadmin@123 lands on the Admin Dashboard.
  - Avatar element ids differ per view: `#nav-avatar` (booking), `#op-avatar` (operator), `#admin-avatar` (admin). Set the right one in each routeForRole branch.
---

## Sun Jun 28 - US-005 User list on the Admin Dashboard
- Replaced the placeholder `loadAdminUsers()` in app.html with a real `GET /api/admin/users` fetch via `apiFetch` (shared authed wrapper: Bearer header + auto-logout on 401). Caches into module-level `adminUsers` (US-006 will refresh this after add), then `renderAdminUsers()` draws the list.
- Each user renders as a `.admin-user-card`: gradient initial avatar + name + email + role badge. Role badges reuse `op-status-badge` styling via a `ROLE_BADGE` map (admin=purple/new `.op-badge--purple`, operator=blue, customer=gray) — visually distinct. All DB text goes through `escapeHtml`.
- Loading ("Loading users…"), empty ("No users yet"), and error ("Could not load users") states all handled, mirroring `loadOperatorTrips`/`renderOperatorTrips`.
- Added CSS: `.op-badge--purple` (alongside the other op-badge variants) and `.admin-user-*` card styles.
- Typecheck clean; full suite 37 pass (frontend-only, no backend/test change).
- **Learnings for future iterations:**
  - Pattern for a new dashboard list = loader fn (fetch via apiFetch, cache to a module var, handle error) + render fn (empty-state vs `.map().join('')` of cards). Mirror `loadOperatorTrips`/`renderOperatorTrips`.
  - Role badge map (`ROLE_BADGE`) + `roleBadgeHtml()` are now reusable for US-006's form/list; `adminUsers` is the cache to re-render after adding a user (call `loadAdminUsers()` to refresh).
  - Browser verification still NOT possible here (no dev-browser/MCP) — manual check needed that the list shows the seeded admin/operators/customers.
---

## Sun Jun 28 - US-006 Add-user form on the Admin Dashboard
- Added an "Add Team Member" form to `admin-view` in app.html (above the user list): Full Name / Email / Password inputs reusing `.auth-field`, plus a Role `<select id="admin-add-role">` limited to Operator/Admin, laid out in a new `.admin-form-card` with a 2-col `.admin-form-grid`. Submit button `#admin-add-submit` reuses `.btn-auth-primary`.
- Added `doAddUser()`: client validation mirrors the server (name/email/password required, password >= 6, role in operator|admin) via `showAuthError/hideAuthError('admin-add-error')`; `setBusy('admin-add-submit', …)` for the in-flight state; POSTs `/api/admin/users` through `apiFetch` (Bearer + auto-logout on 401). On success: clears all fields, resets role to operator, shows a green success banner, and `await loadAdminUsers()` to refresh the list (re-renders from the `adminUsers` cache via the US-005 render path).
- Surfaced server errors: 409 → "That email is already in use." (falls back to server `data.error`), 403 → permission message, otherwise `data.error`.
- Added a green banner: new `.admin-add-success` CSS (mirrors `.auth-error` but green) + `showAddSuccess/hideAddSuccess()` helpers (parallel to showAuthError/hideAuthError). Success label uses `ROLE_BADGE[role].label`.
- Typecheck clean; full suite 37 pass (frontend-only, no backend/test change).
- **Learnings for future iterations:**
  - Reuse the auth-form helper trio for ANY dashboard form: `showAuthError/hideAuthError(id)` for inline red errors, `setBusy(btnId, busy, busyLabel, idleLabel)` for the submit button. There was no green/success equivalent, so `showAddSuccess/hideAddSuccess` were added — reuse those for future success banners.
  - The create response shape is `{ user: {id,name,email,role} }` (data.user), matching admin-routes US-001. Posting via `apiFetch` (not raw fetch) is required so the Bearer token is attached; raw fetch would 401.
  - `ROLE_BADGE` (US-005) doubles as a role label source: `ROLE_BADGE[role].label` gives a human-readable role name.
  - Browser verification still NOT possible here (no dev-browser/MCP) — manual check needed that adding an operator shows it in the list and the new operator can then log in.
---
